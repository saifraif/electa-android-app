<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="clause"
            type="bd.electa.app.models.CharterClause" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tvClauseNumberAndGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{clause.clauseNumber}"
            android:textSize="12sp"
            android:textStyle="italic"
            tools:text="Clause 1.0" />

        <TextView
            android:id="@+id/tvClauseTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@{clause.title}"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Anti-Corruption Pledge" />

        <TextView
            android:id="@+id/tvClauseDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@{clause.description}"
            android:textSize="14sp"
            tools:text="Detailed description of the clause..." />
    </LinearLayout>
</layout>